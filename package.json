{
  "name": "bookreview",
  "version": "1.0.0",
  "description": "Site de reviews de livros",
  "scripts": {
    "start": "concurrently \"node backend/src/server.js\" \"cd frontend && vite\"",
    "dev": "concurrently \"nodemon backend/src/server.js\" \"cd frontend && vite\"",
    "build": "cd frontend && vite build",
    "preview": "cd frontend && vite preview"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.11.3",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0",
    "nodemon": "^3.0.1",
    "vite": "^4.5.0"
  }
}